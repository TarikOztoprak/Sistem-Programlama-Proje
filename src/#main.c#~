#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdlib.h>
#include <sys/wait.h>
#include <sys/types.h>
#include <dirent.h>
#include "jrb.h"
#include "fields.h"
#include "main.h"

int main(int argc, char **argv)
{
  IS is;
  IS is2;
  
  if( argc != 4)
    {
      fprintf(stderr, "HATALI GİRİŞ!");
    }
  /* is = new_inputstruct(argv[2]); */
  /* if (is == NULL){ */
  /*   perror(argv[1]); */
  /*   exit(1); */
  /* } */
  
  return 0;
}


